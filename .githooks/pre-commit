#!/bin/sh
set -e

echo "🔍 [pre-commit] 自动格式化并检查暂存的前端代码..."
# 使用 --filter 精确指定在 frontend 目录运行 lint-staged
pnpm --filter frontend exec lint-staged

echo "✅ 代码检查通过，准备提交..."
